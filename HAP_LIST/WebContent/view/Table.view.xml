<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:haplist="haplist.control"
	xmlns:haplib="hap.lib"
	controllerName="haplist.controller.Table">
	<!--<Table		
		items="{/entities}">				
		<columns>
			<Column
				width="12em">
				<Text text="{i18n>documentType}" />
			</Column>
			<Column>
				<Text text="{i18n>employeeName}" />
			</Column>			
		</columns>
		<items>
			<ColumnListItem>
				<cells>					
					<Text
						text="{docType}" />
					<Text
						text="{name}" />											
				</cells>
			</ColumnListItem>
		</items>
	</Table>-->	
	
	<haplib:DynamicList
		columns="{transfer>/columns}"
		pathToData="/entities">
	</haplib:DynamicList>
</mvc:View>